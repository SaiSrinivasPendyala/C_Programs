//Read a symmetric matrix, store the symmetric elements in another 1-D array.
#include <stdio.h>
int main()
{
	int dimension;
	printf("Please enter the dimension of the matrix: ");
	scanf("%d",&dimension);
	int matrix[dimension][dimension],counter;
	printf("Please enter the elements of the symmetric matrix:\n");
	for(counter=0;counter<dimension;counter++)
	{
		for(iterator=0;iterator<dimension;iterator++)
		{
			scanf("%d",&matrix[counter][iterator]);
		}
	}
	int a[k];
}



(i-1)*(n-(i/2))+j-i = k