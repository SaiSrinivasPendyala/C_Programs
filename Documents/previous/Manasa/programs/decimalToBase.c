//Converting decimal number to given base.
#include <stdio.h>
int convert(int,int);
int main()
{
	int integer,base;
	printf("Integer = ");
	scanf("%d",&integer);
	printf("Base = ");
	scanf("%d",&base);
	converted = convert(integer,base);
	printf("%d in base %d is ")
}